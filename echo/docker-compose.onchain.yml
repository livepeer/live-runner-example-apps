# On-chain payment overlay for echo. Layer it on the offchain base:
#   docker compose -f docker-compose.yml -f docker-compose.onchain.yml up -d --build
#
# Adds the shared remote signer, re-points the orchestrator on-chain (see
# ../compose.onchain.yml), and registers the app with a price so the orchestrator
# issues a payment challenge and meters the session. Requires a local .env
# (gitignored); copy .env.example and fill it in. Then the client reserves with
# the signer and keeps the session funded for the stream:
#   uv run client.py --blur --signer http://localhost:7936 ~/samples/clip.mp4

services:
  signer:
    extends:
      file: ../compose.onchain.yml
      service: signer
    ports:
      - "7936:7936"

  orchestrator:
    extends:
      file: ../compose.onchain.yml
      service: orchestrator

  # Re-declare the command to advertise a price (base file registers free).
  app:
    command:
      - python
      - runner.py
      - --host=0.0.0.0
      - --orchestrator=https://orchestrator:8935
      - --orchSecret=abcdef
      - --runner-url=http://app:8989
      - --price=${PRICE_PER_UNIT}
      - --pixels-per-unit=${PIXELS_PER_UNIT}

# Shared offchain orchestrator service.
#
# Not run on its own. Each example's docker-compose.yml pulls this in with
# `extends` so the orchestrator is defined once for the whole repo:
#
#   services:
#     orchestrator:
#       extends:
#         file: ../compose.orchestrator.yml
#         service: orchestrator
#
# The go-livepeer image entrypoint is `livepeer`, so `command:` is flags only.

services:
  orchestrator:
    image: livepeer/go-livepeer:ja-live-runner
    container_name: example_apps_orchestrator
    # Two distinct addresses:
    #   serviceAddr    advertised to external clients in discovery responses,
    #                  so it must be reachable from the host (published port).
    #   liveRunnerAddr control-plane URL runners use for heartbeat + trickle,
    #                  so it must resolve on the compose network.
    command:
      - -orchestrator
      - -useLiveRunners
      - -serviceAddr=127.0.0.1:8935
      - -httpAddr=0.0.0.0:8935
      - -liveRunnerAddr=https://orchestrator:8935
      - -orchSecret=abcdef
      - -network=offchain
      - -monitor=false
      - -v=6
    ports:
      - "8935:8935"
    # Lets dependents wait for readiness via `depends_on: condition: service_healthy`.
    # 8935 is HTTPS, and the image has no curl/wget — only openssl. A raw-TCP probe
    # (bash /dev/tcp) opens the socket without a TLS handshake, which the server logs
    # as "TLS handshake error: unexpected EOF" every interval. So speak real TLS with
    # openssl and GET /healthz; matching "200 OK" also confirms the orchestrator is
    # actually serving, not just listening.
    healthcheck:
      test: printf 'GET /healthz HTTP/1.0\r\n\r\n' | openssl s_client -connect 127.0.0.1:8935 -quiet 2>/dev/null | grep -q '200 OK'
      interval: 3s
      timeout: 3s
      retries: 40
      start_period: 5s

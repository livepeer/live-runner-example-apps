[project]
name = "livepeer-tiles"
version = "0.1.0"
description = "Tiles (capacity fan-out) example app for the Livepeer network."
requires-python = ">=3.12"
dependencies = [
    "opencv-python-headless",  # runner: stylize tiles; client: split/stitch
    "numpy",
    "aiohttp",
    "livepeer-gateway>=1.0.0",
]

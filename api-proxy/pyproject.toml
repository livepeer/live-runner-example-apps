[project]
name = "livepeer-api-proxy"
version = "0.1.0"
description = "API proxy example app for the Livepeer network."
requires-python = ">=3.12"
dependencies = [
    "aiohttp",
    "livepeer-gateway>=1.0.0",
]

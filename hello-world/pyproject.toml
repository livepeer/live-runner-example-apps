[project]
name = "livepeer-hello-world"
version = "0.1.0"
description = "Hello-world example app for the Livepeer network."
requires-python = ">=3.12"
dependencies = [
    "livepeer-gateway>=1.0.0",
]
